{
  "name": "ai-mindset-blog",
  "version": "1.0.0",
  "description": "A minimalist, lightweight blog",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "serve": "npx serve .",
    "dev": "npm run build && npm run serve"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^4.3.0"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}